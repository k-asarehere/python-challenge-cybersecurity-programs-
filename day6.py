# a program that takes a list of numbers and prints the sum and average 

numbers = [1,2,3,4,5]
summation = sum(numbers) # sum of list of numbers 
average = summation / len(numbers) # average of numbers 
print('Sum:',summation)
print('Average:',average)